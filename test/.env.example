# UniPile credentials
UNIPILE_BASE_URL=https://api1.unipile.com:13111
UNIPILE_API_KEY=your-unipile-api-key

# Telegram chat IDs (UniPile IDs, not Telegram native)
# Run `bun run test/ab-test.ts --list-chats` to find these
BOT_A_CHAT_ID=   # OpenClaw instance WITH context-compress skill
BOT_B_CHAT_ID=   # OpenClaw instance WITHOUT skill (baseline)
